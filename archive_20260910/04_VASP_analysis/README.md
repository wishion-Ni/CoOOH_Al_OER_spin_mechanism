# VASP Analysis

The accepted fixed-geometry formals are under `cohp_bader`, `pdos_dband`, `orbital_assignment`, and `reaction_density`. Use the processed TSV files and validation logs before opening any large raw output. `orbital_assignment` preserves the conservative state-correlation audit; it does not assert a unique d-orbital label where states mix or reorder.
